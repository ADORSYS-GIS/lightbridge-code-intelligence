[package]
name = "lci-agent-tools"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Tool contracts and registry for Lightbridge agents."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
lci-agent-types = { path = "../agent-types" }
tokio.workspace = true
