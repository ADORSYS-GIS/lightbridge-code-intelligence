[package]
name = "lci-agent-step"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Durability seam for Lightbridge agent steps."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
lci-agent-types = { path = "../agent-types" }
tokio.workspace = true
