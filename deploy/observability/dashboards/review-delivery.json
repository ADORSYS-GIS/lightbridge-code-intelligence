{
  "annotations": {},
  "description": "Review delivery and investigation dashboard: task state, outbox status, platform response errors, queued review payloads, and persisted or pending findings.",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 0,
        "y": 0
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) FROM outbox o WHERE o.kind = 'review' AND o.status = 'posted' AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}')",
          "refId": "A"
        }
      ],
      "title": "2xx Posted",
      "transformations": [],
      "transparent": false,
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 6,
        "y": 0
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) FROM outbox o WHERE o.kind = 'review' AND o.status = 'failed' AND o.last_error ~ '[4][0-9]{2}' AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}')",
          "refId": "A"
        }
      ],
      "title": "4xx Client Errors",
      "transformations": [],
      "transparent": false,
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 12,
        "y": 0
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) FROM outbox o WHERE o.kind = 'review' AND o.status = 'failed' AND o.last_error ~ '401|403' AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}')",
          "refId": "A"
        }
      ],
      "title": "401/403 Auth Failures",
      "transformations": [],
      "transparent": false,
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 18,
        "y": 0
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) FROM outbox o WHERE o.kind = 'review' AND o.status = 'failed' AND o.last_error ~ '[5][0-9]{2}' AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}')",
          "refId": "A"
        }
      ],
      "title": "5xx Server Errors",
      "transformations": [],
      "transparent": false,
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 6,
        "x": 0,
        "y": 4
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT t.status AS metric, count(*) AS value FROM tasks t LEFT JOIN repositories r ON r.id = t.repository_id WHERE $__timeFilter(t.created_at) AND t.command_text <> 'index' AND ('${platform}' = 'All' OR r.platform::text = '${platform}') GROUP BY t.status ORDER BY value DESC",
          "refId": "A"
        }
      ],
      "title": "Review task state",
      "transformations": [],
      "transparent": false,
      "type": "bargauge"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 6,
        "x": 6,
        "y": 4
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT o.kind || ' / ' || o.status AS metric, count(*) AS value FROM outbox o WHERE $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}') GROUP BY o.kind, o.status ORDER BY value DESC",
          "refId": "A"
        }
      ],
      "title": "Outbox state by kind",
      "transformations": [],
      "transparent": false,
      "type": "bargauge"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 6,
        "x": 12,
        "y": 4
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT CASE   WHEN rv.task_id IS NOT NULL THEN 'persisted review'   WHEN EXISTS (SELECT 1 FROM outbox o WHERE o.task_id = t.id AND o.kind = 'review'                AND o.status = 'pending') THEN 'queued/backing off'   WHEN EXISTS (SELECT 1 FROM outbox o WHERE o.task_id = t.id AND o.kind = 'review'                AND o.status = 'failed') THEN 'dead-lettered'   WHEN t.error_detail IS NOT NULL THEN 'no post with detail'   ELSE 'no review row/outbox' END AS metric, count(*) AS value FROM tasks t LEFT JOIN repositories r ON r.id = t.repository_id LEFT JOIN reviews rv ON rv.task_id = t.id WHERE t.status = 'succeeded' AND t.command_text <> 'index' AND $__timeFilter(t.created_at) AND ('${platform}' = 'All' OR r.platform::text = '${platform}') GROUP BY 1 ORDER BY value DESC",
          "refId": "A"
        }
      ],
      "title": "Succeeded reviews by delivery state",
      "transformations": [],
      "transparent": false,
      "type": "bargauge"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 6,
        "x": 18,
        "y": 4
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT coalesce(substring(o.last_error FROM '\\b([45]\\d{2})\\b'), 'other') AS metric, count(*) AS value FROM outbox o WHERE o.kind = 'review' AND o.status IN ('pending', 'failed') AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}') GROUP BY 1 ORDER BY value DESC",
          "refId": "A"
        }
      ],
      "title": "HTTP status breakdown",
      "transformations": [],
      "transparent": false,
      "type": "bargauge"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 24,
        "x": 0,
        "y": 12
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "SELECT $__timeGroupAlias(o.created_at, $__interval), o.kind || ' / ' || o.status AS \"series\", count(*) AS \"count\" FROM outbox o WHERE $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}') GROUP BY 1, 2 ORDER BY 1, 2",
          "refId": "A"
        }
      ],
      "title": "Outbox deliveries over time",
      "transformations": [],
      "transparent": false,
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "description": "Review outbox rows that failed or are backing off, with the platform response kept in last_error.",
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "last response"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 10,
        "w": 24,
        "x": 0,
        "y": 20
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT o.created_at AS \"queued\", o.id AS \"outbox id\", o.platform, coalesce(o.owner || '/' || o.repo, t.repository_id::text) AS \"repository\", t.target_type AS \"target\", t.target_id AS \"number\", t.id AS \"task id\", o.status, o.attempts, o.next_attempt_at AS \"next attempt\", o.last_error AS \"last response\", jsonb_array_length(coalesce(o.payload->'comments', '[]'::jsonb)) AS \"inline\", coalesce(jsonb_array_length(o.payload->'findings_json'), 0) AS \"findings\" FROM outbox o LEFT JOIN tasks t ON t.id = o.task_id WHERE o.kind = 'review' AND o.status IN ('pending','failed') AND $__timeFilter(o.created_at) AND ('${platform}' = 'All' OR o.platform::text = '${platform}') ORDER BY o.created_at DESC, o.id DESC LIMIT 100",
          "refId": "A"
        }
      ],
      "title": "Review post failures and retries",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "description": "Shows findings already persisted in reviews plus findings still sitting in review outbox payloads.",
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "body"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 12,
        "w": 24,
        "x": 0,
        "y": 30
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH persisted AS (  SELECT rv.created_at, 'persisted' AS source, NULL::bigint AS outbox_id,          t.id AS task_id, r.platform::text AS platform, r.owner || '/' || r.name AS repository,          t.target_type, t.target_id, f   FROM reviews rv JOIN tasks t ON t.id = rv.task_id   LEFT JOIN repositories r ON r.id = t.repository_id   CROSS JOIN LATERAL jsonb_array_elements(rv.findings) f   WHERE $__timeFilter(rv.created_at)), queued AS (  SELECT o.created_at, o.status AS source, o.id AS outbox_id,          t.id AS task_id, o.platform::text AS platform, o.owner || '/' || o.repo AS repository,          t.target_type, t.target_id, f   FROM outbox o LEFT JOIN tasks t ON t.id = o.task_id   CROSS JOIN LATERAL jsonb_array_elements(coalesce(o.payload->'findings_json', '[]'::jsonb)) f   WHERE o.kind = 'review' AND $__timeFilter(o.created_at)) SELECT created_at AS \"time\", source, outbox_id AS \"outbox id\", platform, repository, target_type AS \"target\", target_id AS \"number\", task_id AS \"task id\", coalesce(nullif(f->>'priority',''), nullif(f->>'severity',''), 'P2') AS \"priority\", coalesce(nullif(f->>'category',''), 'correctness') AS \"category\", f->>'file' AS \"file\", f->>'line' AS \"line\", f->>'title' AS \"title\", f->>'body' AS \"body\" FROM (SELECT * FROM persisted UNION ALL SELECT * FROM queued) x WHERE ('${platform}' = 'All' OR x.platform = '${platform}') ORDER BY created_at DESC LIMIT 300",
          "refId": "A"
        }
      ],
      "title": "Review findings from persisted and queued reviews",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "detail"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 6,
        "w": 24,
        "x": 0,
        "y": 42
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT t.created_at AS \"created\", t.started_at AS \"started\", t.completed_at AS \"completed\", coalesce(r.owner || '/' || r.name, t.repository_id::text) AS \"repository\", r.platform, t.target_type AS \"target\", t.target_id AS \"number\", t.command_text AS \"command\", t.kind, t.preset, t.entry_point, t.status, t.attempts, t.error_detail AS \"detail\", t.head_sha, t.id AS \"task id\" FROM tasks t LEFT JOIN repositories r ON r.id = t.repository_id WHERE ${task_id:sqlstring} <> '' AND t.id::text = ${task_id:sqlstring}",
          "refId": "A"
        }
      ],
      "title": "Selected task state: $task_id",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "last response"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "payload"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 11,
        "w": 24,
        "x": 0,
        "y": 48
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT o.created_at AS \"queued\", o.posted_at AS \"posted\", o.id AS \"outbox id\", o.platform, o.kind, o.status, o.attempts, o.next_attempt_at AS \"next attempt\", o.platform_ref_id AS \"platform id\", o.last_error AS \"last response\", jsonb_array_length(coalesce(o.payload->'comments', '[]'::jsonb)) AS \"inline\", coalesce(jsonb_array_length(o.payload->'findings_json'), 0) AS \"findings\", o.payload::text AS \"payload\" FROM outbox o WHERE ${task_id:sqlstring} <> '' AND o.task_id::text = ${task_id:sqlstring} ORDER BY o.created_at DESC, o.id DESC",
          "refId": "A"
        }
      ],
      "title": "Selected task outbox: $task_id",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "body"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 0,
        "y": 59
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT o.id AS \"outbox id\", c.ord AS \"index\", c.item->>'path' AS \"path\", c.item->>'line' AS \"line\", c.item->>'start_line' AS \"start line\", c.item->>'body' AS \"body\" FROM outbox o CROSS JOIN LATERAL jsonb_array_elements(coalesce(o.payload->'comments', '[]'::jsonb)) WITH ORDINALITY AS c(item, ord) WHERE ${task_id:sqlstring} <> '' AND o.task_id::text = ${task_id:sqlstring} AND o.kind = 'review' ORDER BY o.created_at DESC, c.ord",
          "refId": "A"
        }
      ],
      "title": "Selected task review comment payloads: $task_id",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "body"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 12,
        "y": 59
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH source AS (  SELECT 'persisted' AS source, rv.findings AS findings FROM reviews rv   WHERE ${task_id:sqlstring} <> '' AND rv.task_id::text = ${task_id:sqlstring}   UNION ALL   SELECT o.status AS source, coalesce(o.payload->'findings_json', '[]'::jsonb) AS findings   FROM outbox o WHERE ${task_id:sqlstring} <> '' AND o.task_id::text = ${task_id:sqlstring}   AND o.kind = 'review') SELECT s.source, f->>'file' AS \"file\", f->>'line' AS \"line\", coalesce(nullif(f->>'priority',''), nullif(f->>'severity',''), 'P2') AS \"priority\", coalesce(nullif(f->>'category',''), 'correctness') AS \"category\", f->>'title' AS \"title\", f->>'body' AS \"body\" FROM source s CROSS JOIN LATERAL jsonb_array_elements(s.findings) f ORDER BY source, file, line",
          "refId": "A"
        }
      ],
      "title": "Selected task findings: $task_id",
      "transformations": [],
      "transparent": false,
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${DS_POSTGRES}"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "body"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 10,
        "w": 24,
        "x": 0,
        "y": 69
      },
      "options": null,
      "repeatDirection": "h",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${DS_POSTGRES}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT rc.kind, rc.file, rc.line, rc.platform_comment_id AS \"comment id\", coalesce(nullif(f->>'priority',''), 'P2') AS priority, coalesce(nullif(f->>'category',''), 'correctness') AS category, f->>'title' AS title, f->>'body' AS body FROM review_comments rc JOIN reviews rv ON rv.task_id = rc.task_id CROSS JOIN LATERAL jsonb_array_elements(rv.findings) f WHERE ${task_id:sqlstring} <> '' AND rc.task_id::text = ${task_id:sqlstring} AND f->>'file' = rc.file AND (f->>'line')::int = rc.line ORDER BY rc.file, rc.line",
          "refId": "A"
        }
      ],
      "title": "Selected task: posted inline comments + finding detail",
      "transformations": [],
      "transparent": false,
      "type": "table"
    }
  ],
  "refresh": "1m",
  "schemaVersion": 36,
  "style": "dark",
  "tags": [
    "lightbridge",
    "generated"
  ],
  "templating": {
    "list": [
      {
        "auto": false,
        "auto_count": 30,
        "auto_min": "10s",
        "hide": 0,
        "id": "00000000-0000-0000-0000-000000000000",
        "includeAll": false,
        "label": "Platform",
        "multi": false,
        "name": "platform",
        "query": "All,github,gitlab,bitbucket",
        "skipUrlSync": false,
        "type": "custom"
      },
      {
        "auto": false,
        "auto_count": 30,
        "auto_min": "10s",
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "${DS_POSTGRES}"
        },
        "hide": 0,
        "id": "00000000-0000-0000-0000-000000000000",
        "includeAll": false,
        "label": "Task ID (drill-down)",
        "multi": false,
        "name": "task_id",
        "query": "SELECT t.id::text AS \"__value\", coalesce(r.owner || '/' || r.name, t.repository_id::text) || ' #' || t.target_id   || ' \u00b7 ' || t.status || ' \u00b7 ' || to_char(t.created_at, 'MM-DD HH24:MI') AS \"__text\" FROM tasks t LEFT JOIN repositories r ON r.id = t.repository_id WHERE $__timeFilter(t.created_at) AND t.command_text <> 'index' ORDER BY t.created_at DESC, t.id DESC LIMIT 300",
        "refresh": 2,
        "skipUrlSync": false,
        "type": "query"
      }
    ]
  },
  "time": {
    "from": "now-7d",
    "to": "now"
  },
  "timezone": "browser",
  "title": "Lightbridge \u2014 Review delivery",
  "uid": "lci-review-delivery"
}
