# Architecture Decision Records

This directory records the significant architectural decisions for Lightbridge Code Intelligence,
using the [MADR](https://adr.github.io/madr/) format.

## Process

- An ADR captures **one** decision: its context, the chosen option, and the consequences.
- ADRs are **immutable once accepted**. We do **not** edit the substance of an accepted ADR.
  If a decision changes, write a **new** ADR that **supersedes** the old one, and update the
  superseded ADR's status to `Superseded by ADR-NNNN`.
- Status values: `Proposed`, `Accepted`, `Rejected`, `Deprecated`, `Superseded by ADR-NNNN`.
- New ADRs start from [the template](0000-adr-template.md) and are numbered sequentially with a
  kebab-case title, e.g. `0014-some-decision.md`.
- Substantial proposals are often socialized first as an [RFC](../rfc/README.md); an accepted RFC
  typically yields one or more ADRs.

## Index

| # | Title | Status |
|---|---|---|
| [0000](0000-adr-template.md) | ADR template | — |
| [0001](0001-use-github-app.md) | Use a GitHub App (not a PAT-backed bot) | Accepted |
| [0002](0002-rust-control-plane-trust-boundary.md) | The Rust control plane owns the trust boundary | Accepted |
| [0003](0003-dual-retrieval-neo4j-pgvector.md) | Dual retrieval: Neo4j + pgvector are complementary | Accepted |
| [0004](0004-one-k8s-job-per-task.md) | One Kubernetes Job per task for execution isolation | Accepted |
| [0005](0005-cratestack-schema-first-control-plane.md) | Adopt cratestack (schema-first Rust) for the control plane | Accepted |
| [0006](0006-nextjs-app-router-web-ui.md) | Next.js (App Router) for the web UI | Accepted |
| [0007](0007-better-auth-rust-backend-plugin.md) | better-auth for web auth via a rust-backend plugin | Superseded by ADR-0014 |
| [0008](0008-adopt-ai-governance-framework.md) | Adopt the ADORSYS-GIS AI Governance framework | Accepted |
| [0009](0009-pnpm-turborepo-monorepo.md) | pnpm + Turborepo monorepo layout | Accepted |
| [0010](0010-graphify-treesitter-indexing-baseline.md) | Graphify + tree-sitter as the indexing baseline | Accepted (graph half superseded by 0086) |
| [0011](0011-engineering-practices-xp-lean-devops.md) | Engineering practices: XP + Lean + DevOps + shift-left | Accepted |
| [0012](0012-rfc-process-alongside-adrs.md) | RFC process alongside ADRs | Accepted |
| [0013](0013-local-dev-and-build-tooling.md) | Local dev & build tooling (just, xtask, compose, nextest, wiremock) | Accepted |
| [0014](0014-keycloak-oidc-resource-server.md) | Keycloak OIDC — web client + resource server | Accepted |
| [0015](0015-web-console-design-language.md) | Web console design language & component system (shadcn/ui) | Superseded by ADR-0027 |
| [0016](0016-dashboard-information-architecture.md) | Dashboard information architecture & task-run views | Accepted |
| [0017](0017-agent-runner-control-plane-bootstrap.md) | Agent runner bootstraps from the control plane (no App key in the Job) | Accepted |
| [0018](0018-openai-compatible-embeddings.md) | OpenAI-compatible API for all embeddings (eaig/core-gateway; no bundled model) | Accepted |
| [0019](0019-graphify-cli-structural-graph.md) | Graphify (bundled CLI) extracts the structural graph; control plane writes Neo4j | Superseded by 0086 |
| [0020](0020-mcp-servers-via-control-plane.md) | MCP servers are thin clients of the control-plane retrieval API (no datastore creds in the Job) | Accepted |
| [0021](0021-opencode-headless-review-agent.md) | OpenCode (headless `run`) is the review agent; generated opencode.json wires eaig + MCP | Superseded by 0026 |
| [0022](0022-review-writeback-control-plane.md) | Control plane validates findings against the PR diff and posts the review (no GitHub creds in the Job) | Accepted |
| [0023](0023-db-backed-rbac.md) | Permission-based authz: the token carries a permissions list under a configurable claim (aud still verified); enforce per-capability; no roles/DB | Accepted |
| [0024](0024-web-console-redesign-v2.md) | Web console redesign v2 — richer surfaces, grouped nav + ⌘K, runs table, insights (within the ADR-0015 contract) | Accepted |
| [0025](0025-review-reuses-base-index.md) | Review reuses the base index instead of re-indexing every run (perf) | Accepted |
| [0026](0026-native-review-agent.md) | Native Rust review agent with structured tool calls; drop OpenCode (robust output + control tools) | Accepted |
| [0027](0027-daisyui-design-system.md) | Adopt daisyUI (dracula theme) as the component layer; supersede ADR-0015's bespoke component/token mechanism | Accepted |
| [0028](0028-agent-job-control-sidecar.md) | Agent Job = control sidecar + single configurable main container (closed built-in pipeline) | Superseded by ADR-0026 |
| [0029](0029-focused-review-not-generic-runner.md) | Scope boundary: a focused code-review system, not a generic step/CI runner (reject arbitrary steps, CRD/operator, workflow engines) | Accepted |
| [0030](0030-repo-review-config.md) | Repo-level review configuration (`.lightbridge-code-review.jsonc`) — extend understanding, not execution | Proposed |
| [0031](0031-review-skills-commands.md) | Custom review skills/commands via the repo config (named prompts, `@mention`-invokable) | Proposed |
| [0032](0032-review-finding-priority-and-category.md) | Review findings carry a priority (P0–P2) + category (security always red), reviewed across all dimensions | Accepted |
| [0033](0033-inbound-command-parsing-and-run-kinds.md) | Parse the `@mention` comment body; run kinds (review / conversational `ask` / skill) + non-PR targets | Accepted |
| [0034](0034-agent-run-transcript-and-observability.md) | Persist the agent run transcript (tool calls, reasoning, tokens) and surface it in the dashboard | Superseded by [0100](0100-retire-db-transcript-logs-as-observability.md) |
| [0035](0035-review-feedback-signal.md) | Capture 👍/👎 on posted reviews as a quality feedback signal (persist comment IDs, store + display) | Accepted |
| [0036](0036-auto-read-agent-instruction-files.md) | Auto-read conventional agent instruction files (AGENTS.md → CLAUDE.md → …) as review context, ranked, untrusted | Accepted |
| [0037](0037-agent-acts-via-mediated-tools.md) | The agent acts via mediated write tools (add_review_comment / add_comment / …); run kind is emergent, not classified up front | Accepted |
| [0038](0038-per-repo-review-model.md) | Per-repository review model selected in the admin UI from an operator allowlist (admin-owned, not the author-owned repo file) | Proposed (scope expanded → per-identity + ACL, see #241) |
| [0039](0039-agent-llm-resilience-and-observability.md) | Agent LLM resilience & observability: generous per-request timeout (180s, eaig reality), bounded retry/backoff on transient errors, per-run circuit breaker, optional model failover, captured HTTP error body, structured per-turn logging | Accepted |
| [0040](0040-re-review-reads-prior-findings.md) | A re-review reads the agent's own prior review as context (reconcile, don't contradict) | Accepted |
| [0041](0041-full-diff-coverage-gate.md) | Full-diff coverage gate: bounce an early finish while changed files are untouched | Accepted |
| [0042](0042-risk-first-review-and-parallel-batching.md) | Risk-first review strategy + parallel read-only tool batching + cumulative read budgets (Phase 1) | Accepted |
| [0043](0043-review-finding-verification.md) | Finding verification: evidence citation on every finding + a refute pass on P0/P1 (Phase 2) | Accepted |
| [0044](0044-feedback-memory-m1.md) | Feedback memory (M1): inject 👎-rejected findings as repo memory so the agent doesn't re-raise known false positives | Accepted |
| [0045](0045-context-window-budget.md) | Context-window budget: converge before overflow, trim consumed tool output, never discard buffered findings | Accepted |
| [0046](0046-observability-dashboard-deployment.md) | Observability dashboard deployment (most dashboards read Postgres, not Prometheus) | Accepted |
| [0047](0047-review-prompt-grounding-and-uncertainty.md) | Reviewer prompt — grounding & uncertainty calibration (empty retrieval ≠ proof of absence) | Accepted |
| [0048](0048-review-prompt-structure-and-technique.md) | Reviewer prompt — structure & technique adapted to a GLM / OpenAI-compatible model | Accepted |
| [0049](0049-eval-driven-reviewer-prompt-iteration.md) | Eval-driven reviewer-prompt iteration — golden cases before deploy | Accepted |
| [0050](0050-retrieval-pins-to-latest-indexed-snapshot.md) | Reviews reuse the latest indexed snapshot (pin retrieval + skip-check to it; no per-PR re-index) | Accepted |
| [0051](0051-per-model-config.md) | Per-model configuration blocks (primary / fallback / embeddings) | Accepted |
| [0052](0052-index-snapshot-pruning.md) | Index snapshot pruning — keep the latest + in-flight, sweep the rest | Accepted |
| [0053](0053-remove-review-fallback-model.md) | Remove the review fallback model (single model + retry/breaker; failover dropped) | Accepted |
| [0054](0054-review-model-and-provider-selection.md) | Stay on MiniMax-M2.7 (FP8) on DeepInfra for the review agent | Accepted |
| [0055](0055-review-waits-for-index-readiness.md) | A review waits for index readiness (the `WaitingForIndex` gate; RFC-0002 Phase 1.4) | Accepted |
| [0056](0056-control-plane-owns-the-posted-output.md) | The control plane owns what gets posted (PR review-only channel + failure notice) | Accepted |
| [0057](0057-poller-posts-failure-notice-on-uncatchable-kill.md) | The poller posts the failure notice for an uncatchable kill (closes the ADR-0056 gap) | Accepted (egress superseded by 0059) |
| [0058](0058-rename-poller-role-to-reconciler.md) | Rename the `poller` role to `reconciler` (bidirectional GitHub reconciliation) | Accepted |
| [0059](0059-reconciler-owns-all-github-egress.md) | The reconciler owns all GitHub egress via a transactional outbox (one writer) | Accepted |
| [0060](0060-capture-model-reasoning-and-glm-5-2-latency-finding.md) | Capture the model's reasoning (`reasoning_content`) to logs + the GLM-5.2 latency finding (prod reverted to M2.7, realigning 0054) | Accepted |
| [0061](0061-sast-deterministic-finding-source.md) | SAST (opengrep) as a deterministic finding source — runs in the runner, rides the existing channel (not reviewdog), LLM-aware not LLM-gated | Superseded by ADR-0073 |
| [0062](0062-two-tier-review-fast-auto-deep-on-demand.md) | Two-tier review: fast auto pass (SAST + a lean diff-only LLM pass) on every PR; deep on-demand (`@mention`, full retrieval, 2h) — one model, two loop shapes | Accepted |
| [0063](0063-cli-only-repository-approval.md) | CLI-only repository approval (as-built: an Authorization-Code+PKCE ratatui TUI, `clients/lci`) over the existing OIDC-gated endpoints — the retire-the-web-app conclusion is superseded by ADR-0112; the TUI itself, its authz reuse, and the flagged imperative-vs-GitOps tension stand unchanged | Superseded by [0112](0112-invest-in-apps-web-supersede-0063.md) |
| [0064](0064-observability-via-grafana-behind-caddy-oauth2.md) | Observability via Grafana behind a Caddy + oauth2 proxy — retire the bespoke web dashboards (transcript/feedback → Grafana panels) | Proposed |
| [0065](0065-re-review-dedup-and-reconciliation.md) | Re-review must not duplicate: short-circuit unchanged commits + dedup findings at finalize + re-derive-then-reconcile (refines ADR-0040) | Proposed |
| [0066](0066-deep-tier-external-knowledge-tools.md) | External-knowledge MCP tools, mediated by the control plane: a dynamically-discovered, config-driven registry of in-cluster MCP servers (`knowledge_tools.mcp_servers`, e.g. brave-search/context7) — no hardcoded per-provider tools, query/name not URL (keeps the Job sandboxed, no new secrets); any tier via the normal `review.<tier>.tools` allowlist, which picks discovered tools by `mcp__`-anchored regex selectors (not a blunt on/off), not deep-only | Accepted |
| [0067](0067-skip-auto-review-on-bot-authored-prs.md) | Skip the automatic fast review when the PR author is a bot (`user.type == "Bot"` + `[bot]` backstop, fail open); `@mention` deep review untouched; `review.skip_bot_authored_prs` defaults on | Accepted |
| [0068](0068-reaction-driven-review-lifecycle.md) | Reaction-driven review lifecycle: 👀 on work-started (not receipt), 👍 clean+silent (no comment), 👎 findings (❌→👎, GitHub has no ❌), 😕 failure — all on the trigger (PR body or the `@mention` comment via `tasks.trigger_comment_id`); silent-clean supersedes ADR-0056's never-silent for the clean case | Accepted |
| [0069](0069-review-tier-minimum-model-capability.md) | Review tiers carry a minimum model-capability floor (deep = frontier-class reasoning model; flash/lite is below it) and the coverage gate verifies instead of trusting: bounce with a cap + stalled-re-finish callout + a coverage disclosure appended to an incomplete finish (refines ADR-0041; incident run `bac4b5d8`) | Accepted |
| [0070](0070-window-proportional-prompt-budgets.md) | Window-proportional budgets for the injected static context blocks (diff/SAST/priors/memory/instructions): each block's char budget = min(absolute ceiling, share-of-window), floored so framing survives, cut-and-disclosed — reuses the `context_window` knob (no new config); no window → ceilings unchanged (legacy) | Accepted |
| [0071](0071-multi-line-review-comments.md) | Multi-line (range) inline review comments — additive `start_line`/`start_side` on the existing single-line comment payload, validated as a contiguous commentable range within one hunk, falling back to single-line rather than dropping the finding (refines ADR-0022's named follow-up) | Proposed |
| [0072](0072-platform-abstraction-layer.md) | Platform-abstraction layer (`CodePlatform` trait) for GitHub + GitLab — one async trait, two implementations, unified `/webhook` route + `outbox` table with a `platform` column; agent-runner untouched; phases 0–3 are pure refactors (controlled breaking change: `github_delivery_id`→`webhook_delivery_id` requires frontend update) | Accepted |
| [0073](0073-sast-as-agent-tool.md) | SAST (opengrep) becomes an agent-called `run_sast` tool, not a deterministic pre-agent pass — runs only when the agent invokes it (fixes scanning on conversational runs; selectable per-tier via `review.<tier>.tools`), knowingly trading ADR-0061's deterministic guarantee for LLM-gated on-demand scanning on both tiers | Accepted |
| [0074](0074-restate-egress-pilot.md) | Restate pilot (RFC-0005 Phase A): platform egress becomes a `PlatformEgress` virtual object keyed per `platform:installation` — makes ADR-0059's single-writer structural; outbox table stays as ledger/dead-letter; entry gate (spike) + 3-week exit gate + config-flag rollback | Proposed |
| [0075](0075-rig-for-new-agent-surfaces.md) | Rig for new agent surfaces — **narrowed by the spike (#300/#303):** the eaig-facing OpenAI-compatible / Chat Completions path DROPS Gemini-3 `thought_signature` (hard 400 on multi-turn tool use, #262 family), so Rig is **NOT adopted** for eaig-backed tool-use surfaces; new surfaces (e.g. A2A `ask`) use the native transport (`chat.rs`), the review loop stays native (ADR-0026), revisitable only on upstream `extra_content` passthrough or eaig exposing the Responses API | Accepted (narrowed) |
| [0076](0076-restate-task-lifecycle-workflow.md) | Restate Phase B (RFC-0005): the task lifecycle becomes one Restate `workflow` per task (id = idempotency tuple + `run_epoch`), hosted by `restate-worker` — replaces dispatcher-claim + reaper + `waiting_for_index` with dedup→index-promise→launch→completion-awakeable-racing-a-deadline→finalize+egress (Phase A); no in-place `tasks` migration (both write, one owns per creation-flag), cross-engine index release via the internal API; gated on ADR-0074's live exit gate; unblocks A2A Phase 4 + Phase C | Proposed |
| [0077](0077-a2a-streaming-event-log.md) | A2A streaming (RFC-0006 Phase 2): an append-only, per-task sequence-numbered `a2a_task_events` log that SSE handlers replay-then-tail — no in-pod fan-out state, so the `a2a` role stays horizontally scalable; status/artifact events projected from `set_task_status`/finalize (pipeline untouched), ordering + cross-subscriber consistency + reconnect-replay + terminal-close guaranteed by the log (R6); Postgres-only (no RFC-0005 dep); polling unchanged; push/`input-required`/`ListTasks` stay later-phase | Proposed |
| [0078](0078-a2a-natural-language-text-part.md) | A2A `review` accepts a natural-language `text` part alongside the `data` part (RFC-0006) | Proposed |
| [0079](0079-a2a-push-notifications-webhook-egress.md) | A2A push notifications via an SSRF-guarded webhook egress (RFC-0006 Phase 3) | Accepted |
| [0080](0080-runner-native-builds-daemonless-images.md) | Compile on the runner, assemble images daemonless (static-musl + mimalloc + buildah); COPY-only Dockerfiles, no Docker daemon; supersedes the sccache-in-Docker plumbing (#327) | Proposed |
| [0081](0081-a2a-input-required-and-list-tasks.md) | A2A Phase 4 (RFC-0006): `ListTasks` (caller-scoped, keyset-paginated read — ships first, needs only the `AND caller_id` predicate `store.rs::list` lacks) split from `input-required` (task parks on a Restate awakeable carrying a question artifact, resumes via a same-`taskId`/`contextId` continuation `SendMessage` that resolves it through the internal API; upgrades ADR-0078's guided rejection into clarify-then-confirm); `input-required` hard-gated on ADR-0076 Phase B, ListTasks is not | Proposed |
| [0082](0082-restate-durable-agent-runtime.md) | Restate Phase D (RFC-0005, new phase — the stated end goal of adopting the engine): the deep-tier review loop becomes a durable `ReviewAgent` workflow — every LLM call + tool batch a journaled `ctx.run` step, so a crash resumes at the step it left (O(1-turn) cost, not O(run)); **designed around a code revamp**: an edition-2024/resolver-v3 `agent-core` crate FAMILY (`agent-types`/`-step`/`-tools`/`-clients`/`-loop`/`review-agent`+dev `agent-testkit`; normative companion doc `docs/restate-phase-d-agent-core-architecture.md` has the DAG, trait signatures + dyn/static decisions, StepError taxonomy, migration map, R1a–R1e slicing) with trait seams (`StepRuntime` Passthrough/Restate/Checkpoint, `Tool` w/ kind+replay-safety metadata + registry views, `TurnPolicy` for budgets/wind-down/coverage, `ModelClient`, `Workspace` lazy-checkout; flows stay functions) — the R1 extraction is a pure un-gated Job-path refactor (golden-transcript merge bar), so the durable runtime is a swap not a rewrite; hosted by a new `agent-worker` role (fast/index keep the ADR-0004 Job; SAST stays sandboxed in a Job); journal-offload rule + per-tool idempotency table; R2 gated G0–G4; fallback = `CheckpointRuntime` (Option B) on the same seam. **⚠️ Amended 2026-07-12 ([RFC-0007](../rfc/0007-control-plane-v2-planes.md)): R1 stands; the R2 `RestateRuntime`-in-loop endpoint is SUPERSEDED by [ADR-0087](0087-durable-replay-checkpoint-runtime.md) (`CheckpointRuntime` — Restate stays on egress, not the loop); `agent-worker` generalized into the [ADR-0085](0085-agent-execution-plane.md) agent-plane.** | Accepted (R1 stands; **R2 endpoint superseded by ADR-0087**) |
| [0083](0083-platform-crate-architecture-and-cratestack-data-layer.md) | Platform crate architecture (extends ADR-0082 to every component; amends ADR-0005): the ~25.5k-line control-plane decomposes into domain crates `lci-{data,platform,egress,queue,a2a}` + thin host (deps pinned to single manifests: cratestack-pg/sqlx→data, kube→queue); **NEW `shared` role** = all six roles composed in one process for single-node/customer installs, with **Postgres advisory locks making every singleton (egress drain, notifier claim, dispatcher ticks) structural** instead of replicas:1 comments; **cratestack 0.4.9 adopted as the high-level ORM** (ORM-only: `include_server_schema!` delegates behind per-domain repository traits, generated routes NOT mounted, raw-SQLx escape hatch for SKIP-LOCKED/NOTIFY/CTE/pgvector residue, `.cstack` regenerated from the live 27-migration schema + CI drift gate, existing migration pipeline kept); P0–Pn sequencing with P1 = cratestack parity spike (fallback: repos stay raw SQLx); **workspace tooling baseline** (bon=builder idiom, thiserror, clap+env, config-rs contract-preserving, camino, cargo-deny, rustfmt.toml, schemars, insta, proptest, cargo-llvm-cov; rayon targeted-indexer-only; fff=future agent-search tool + monty=future run_python sandboxed-compute tool, both experiment-gated; dir-structure+fp-core declined) + **macro escalation ladder** (bon → macro_rules! ≥3× → lci-macros w/ trybuild+expand snapshots; ReviewTool derive + delegate_repo! + step_names! candidates); **test discipline §6**: proptest properties (macro-generated behavior, SSRF validator, policy laws) + cargo-llvm-cov gate = hard ≥85% floor on NEW lci-* crates, RATCHET on legacy (anti-theater per the ADR-0069 lesson), generated code excluded; risks R20–R25 | Proposed |
| [0084](0084-customer-handbook-modular-asciidoc.md) | Customer handbook as modular AsciiDoc: `docs/handbook/` of tiny one-concept `.adoc` files (≤ ~80 lines, stable anchors) composed ONLY by `handbook.adoc`+`nav.adoc`; `attributes.adoc` owns every version/product string (release bump = one line); generated `configuration-reference.adoc` via xtask (docs-as-code, ADR-0046 doctrine); built by asciidoctor(+pdf) in the docker-asciidoctor image via `just handbook`, CI attaches HTML+PDF to each GitHub release for customer delivery; Antora considered/deferred (layout kept Antora-compatible); customer-voice-only rule, one-way linking from contributor docs | Proposed |
| [0085](0085-agent-execution-plane.md) | The agent execution plane ([RFC-0007](../rfc/0007-control-plane-v2-planes.md)): consolidate the scattered `agent-runner`/`agent-review` images (and the proposed `agent-worker`) into ONE `agent-plane` binary selected by **mode × host** — `{index, review, open} × {run-once, serve}` — so Job-vs-worker becomes a per-mode deployment knob, not an architecture bet; a structural trust boundary (agent-plane holds checkout + LLM key + runner token, NEVER DB/forge; journals + acts only via the mediated internal API); routing rules make `open`/execution `run-once`-only (namespaces isolate files, not code); enabled by the Graphify removal (ADR-0086) collapsing the #207 image split | Proposed |
| [0086](0086-in-house-code-graph-crate.md) | In-house Rust code-graph crate `lci-codegraph` retires **Graphify** (the Python dep behind the ~4Gi index Job + the #207 image split): tree-sitter-direct structural graph (symbols + defs/refs + cross-file call edges, reusing `chunker.rs`, preserving the `graph_find_symbol`/`graph_get_callers` contract), co-located chunking+graph+embedding over one parse (unchanged qwen3-embedding-8b path), PDF text extraction, operator-configurable gitignore-style ignore-list; dissolving the Python dep is what makes ADR-0085's single binary viable; parity risk mitigated by golden fixtures + side-by-side diff vs Graphify | Accepted — Implemented |
| [0087](0087-durable-replay-checkpoint-runtime.md) | Durable replay via **`CheckpointRuntime`** (the third `StepRuntime` seam impl): each completed step's result persists to a `durable_step` store `(task_id, run_epoch, step_name)` via the mediated internal API (agent stays DB-less; the orchestration `replay` role owns the store); a pod death replays completed steps and continues live at the first gap — resume inside the isolated Job, no Restate in the loop; success-purge + configurable TTL sweep; dedup keys on the at-least-once write steps; **supersedes ADR-0082's `RestateRuntime`-in-loop** (Restate kept for egress/lifecycle); host-agnostic (run-once or serve) | Proposed |
| [0088](0088-open-mode-autonomous-ticket-agent.md) | The `open` mode: an autonomous ticket→PR agent with write access ([RFC-0007](../rfc/0007-control-plane-v2-planes.md)/ADR-0085's third mode). @mention on an Issue → dispatcher launches `mode=open`, `host=run-once` ALWAYS (executes untrusted + generated code; a shared tenant can't sandbox execution); hardened per-task pod sandbox (non-root/seccomp/read-only-root, egress allowlist, default-deny cluster-internal); credential-light trust boundary — commits to a local branch, hands the patch to the egress plane which holds forge creds and opens the PR (extends ADR-0037 from comments to code); CheckpointRuntime replay with a `propose_pr` dedup key; mandatory human PR gate + AI-usage declaration | Proposed |
| [0089](0089-embeddings-on-the-code-graph.md) | Embeddings on the Neo4j code graph (**amends** ADR-0003, does NOT supersede — pgvector stays): `lci-codegraph` embeds each `:Symbol`'s def span via the same qwen3-embedding-8b/4096-dim eaig path and the control plane stores it as `:Symbol.embedding` behind a Neo4j 5.26 cosine HNSW vector index (4096 = its max dim), enabling graph-native symbol-level vector search composable with the call graph. Additive: pgvector's **exact chunk index untouched** (Neo4j = structure + symbol semantics; pgvector = chunk semantics). Cost: ~2× index-time embedding (symbols≈chunks; Option B reuse-chunk-embeddings is the escape hatch); this lane's recall is approximate (HNSW). Rejected: `:Chunk`-in-Neo4j (dupes pgvector) and drop-pgvector (trades exact→approximate on the quality-critical path, [#285](https://github.com/vymalo/lightbridge-code-intelligence/issues/285)). | Proposed |
| [0090](0090-hybrid-retrieval-tools.md) | Hybrid retrieval tools (semantic + structural), builds on ADR-0089: add ONE control-plane-mediated `lightbridge_graph_semantic_search` — vector search over `:Symbol(embedding)` + exact `:Symbol.label` name match + optional N-hop graph expansion, in a single call (query embedded runner-side, `POST /internal/tasks/{id}/graph_search`, Neo4j creds in the trust boundary). Registered in the per-tier allowlist **alongside** the retained `search`/`graph_find_symbol`/`graph_get_callers` — none removed; deep-tier default, fast-tier opt-in. Fewer turns + "semantic and exact" grounding; cost = one more tool in the choice/window budget (ADR-0070) + approximate recall; measure displacement via ADR-0069 telemetry. | Proposed |
| [0091](0091-refute-pass-outward-disconfirmation-search.md) | Refute pass (ADR-0043) refinement: for a P0/P1 finding claiming something is never sent/set/present, the one-shot refute nudge now directs the model at files it has NOT yet engaged — the transport/interceptor/middleware/config-default layer — instead of only re-reading the finding's own cited evidence, which structurally can only confirm it (#304, webank-mobile#145 false-positive P1). Additive to non-absence findings; same one-shot/P0P1-only cost ceiling. | Accepted |
| [0092](0092-per-task-runner-tokens.md) | Per-task runner tokens (hardens ADR-0017): the shared, long-lived `AGENT_RUNNER_TOKEN` bearer — one value valid against every task's internal-API calls, indefinitely — is replaced by a self-verifying HS256 JWT minted per task at dispatch (`tid` + `exp` ≤ the Job's own `activeDeadlineSeconds`), signed by a control-plane-only `RUNNER_TOKEN_SIGNING_KEY` that never enters a Job; `RunnerAuth` now also rejects a validly-signed token whose `tid` doesn't match the request's own task id (403, cross-task replay) (#243) | Accepted |
| [0093](0093-restate-egress-pilot-no-go.md) | Restate egress pilot **no-go**, superseding ADR-0074: a ~6-day prod soak was operationally flawless (622 posts, 0 dead-letters/errors, 0 restarts) but the pilot's whole rationale — letting the reconciler scale past `replicas: 1` without breaking single-writer ordering — is unused (reconciler is `replicas: 1` at ~89 posts/day, so the invariant is already free and the outbox already gave the ledger/dedup/retry). Revert forge egress to the reconciler drain (ADR-0059) as the sole path, disable the Restate infra (config), and remove the Restate code (#433). Re-adopt only if egress must scale past one writer; RFC-0005 Phase B not pursued on this evidence. | Accepted |
| [0094](0094-opencode-acp-open-mode-host.md) | OpenCode-over-ACP hosts the `open`-mode agent loop ([RFC-0009](../rfc/0009-opencode-acp-agent-host.md) Phase 1): the agent-plane becomes a supervisor that spawns `opencode acp` **inside the unchanged ADR-0088 sandbox** (same pod spec, same credentials — LLM key + task-scoped runner token only; `propose_pr` stays a mediated MCP tool to the egress plane); OpenCode brings edit/bash/sessions/subagents/MCP-client for free; durability = restart-on-failure (no step replay — ADR-0087 not extended to hosted modes; egress dedup keys carry idempotency); `review`/`index` explicitly stay native pending the Phase-2 eval harness | Proposed |
| [0095](0095-opencode-plugins-recording-and-gates.md) | Recording + gate enforcement live in first-party OpenCode plugins (`integrations/opencode/plugins/*`): a **recorder** (in-process `tool.execute.before/after` + reasoning parts off the event bus → right-bytes JSONL → ADR-0034 transcript store; captures subagent-internal calls an ACP client can't see) and a **gate-interlock** (per-session state; `tool.execute.before` **throws** on the terminal tool until refute/coverage preconditions hold — block-until enforcement on the *stable* hook, since `chat.params` can't express `force_names()`-style force-now; `experimental.*` hooks carry steering only, never enforcement); OpenCode version-pinned, every bump re-runs the RFC-0009 probe | Proposed |
| [0096](0096-mediated-forge-read-tools.md) | Forge **read** tools on the mediated MCP surface — the App key stays control-plane-side. The agent (open/review, incl. the ADR-0094 OpenCode host + its read-only `explore` subagent) reads other branches/issues/PRs/files-at-ref via curated read-only tools on the existing `lightbridge` MCP endpoint it already calls (task-scoped runner token); the control plane performs the forge call with a read-only, **task-repo-scoped**, ≤1h installation token it mints+refreshes **server-side**. Pod gains the capability with **zero new credential and zero new egress** (never holds a token, never reaches api.github.com) and no token-expiry failure on long tasks. Curated on the ADR-0072 `CodePlatform` client (multi-forge, tight surface) rather than adopting github-mcp-server in-tree (kept as `--read-only` fallback). Extends ADR-0037/0020 from writes to reads. Rejects: scoped-token-in-pod (widens ADR-0088 egress+creds; token expiry) and App-key-in-pod (master credential in the untrusted-code sandbox — hard no). | Proposed |
| [0097](0097-review-runs-on-opencode.md) | **Review runs on OpenCode** ([RFC-0009](../rfc/0009-opencode-acp-agent-host.md) Phase 3 — reverses its Phase-2/3 phasing): a thin transport host over a **reused Rust core**. OpenCode owns its agent loop; one `session/prompt` runs a whole cycle. Native policies split — budgets/wind-down/batching (ADR-0042) → OpenCode's loop; the **coverage + refute gates (ADR-0041/0091) → supervisor-side, reusing the exact `TurnPolicy` code** (no TS reimplementation). Load-bearing: the gate input is the recorder JSONL not ACP updates (sees subagent-internal reads); built-in tools disabled at the config **TOP LEVEL** not per-agent (ACP runs the default `build` agent, so a per-agent block is ignored → the model would get built-in `read`/`grep` and escape coverage); stdio review MCP via the config `mcp` block; the cutover gate is a **shadow parity run** (`xtask shadow diff`), not the #252 eval harness. Host built + proven vs real OpenCode 1.18.3 (#442–#449); shadow gate #450; **live dispatch cutover (slice 5) pending, shadow-gated**. Does not yet supersede ADR-0026 (native review) until the cut. | Accepted (host built/proven; live cutover slice-5 shadow-gated) |
| [0099](0099-operator-opencode-config-overlay.md) | **Operator-supplied OpenCode config overlay for review** (builds on ADR-0097, reaffirms its #6 checkout-isolation): move the review config from a Rust `json!` blob to a checked-in OpenCode-native `review.jsonc` (like `open` mode), and let a **trusted SysAdmin** (ai-helm-values, NOT the untrusted checkout) supply a `review.opencode` overlay that is **deep-merged host-side with FULL OVERRIDE** (overlay wins on every key). Three documented layers: base file → runtime injection (per-task prompt via `{file:*}`, attribution headers, tier `reasoning`, temperature) → operator overlay. Full override can weaken the coverage/read-only floor (custom sub-agents with different access models); the system **warns + coverage-discloses** when the floor is breached rather than forbidding it (owner's choice: awareness over prevention). Constraints documented for the SysAdmin: opencode's schema **rejects unknown keys** (a typo fails the whole config → review won't start); secrets stay `{env:*}`; checkout is still never a config source. Complements the mediated ADR-0066 MCP path (safe default) as the power-tool for trusted operators. | Accepted (design; implementation follow-up) |
| [0100](0100-retire-db-transcript-logs-as-observability.md) | **Retire the DB run transcript; Loki logs are the single observability surface** (supersedes ADR-0034, epic #459 pivot). On the OpenCode-over-ACP path we don't own the loop, so the recorder→`transcript_from_recorder` reconstruction was a post-hoc record, not a replay seam; its only reader was the `clients/lci` TUI Run Detail view (also removed). Hard cutover: both writers, the `POST/GET /tasks/{id}/transcript` endpoints, `replace_transcript`/`get_transcript`, `TranscriptEntry`, the TUI transcript view, the `RecorderEvent.part`/`reasoning.part` recorder branch, and a `DROP TABLE agent_transcript` migration (0032, reverts 0014/0017). **Kept:** the recorder file + quality gates (a separate consumer), and the native path's `agent reasoning`/`agent content` log lines. The OpenCode logger plugin gains leveled per-turn lines under #462/#463. | Accepted (owner-directed) |
| [0101](0101-ci-arc-pod-runners-no-dind.md) | **CI moves to ARC pod runners; dind is rejected as a Docker-in-CI fix** — the `vymalo-vps` self-hosted label now resolves to ephemeral ARC pods (image `ghcr.io/vymalo/arc-runners:jdk21-node22`, rootless buildah/podman/BuildKit baked in, no Docker daemon), provisioned by the separate `home-os` infra repo. Drops the VM-workaround `container:` wrappers in `rust-lint.yml`/`image-pipeline.yml`, converts `dashboards.yml`'s `python:3.12-bookworm` container to the runner's own venv'd python3, and converts `control-plane-tests.yml`'s `services: postgres` into a rootless `podman run` + readiness wait. Standing rule: any future Docker-needing job adapts to podman — no privileged dind sidecar. Documents two node-level prerequisites on the `netcup-k8s` Talos cluster (namespace PodSecurity `privileged` override, `user.max_user_namespaces` sysctl), verified live. | Accepted |
| [0102](0102-grafana-loki-embedded-run-logs.md) | **Run logs move from a native k8s pod-log stream to an embedded Grafana/Loki panel** — `apps/web`'s run-detail page drops its `@kubernetes/client-node`-backed log stream (RBAC-heavy, dies once the ephemeral pod is GC'd) for a `d-solo` iframe embed of the generated `task-runs` dashboard's "Logs for $task_id" Loki panel, extending ADR-0100's Loki-is-the-surface decision to the frontend. `NEXT_PUBLIC_GRAFANA_URL` (client-visible) gates the embed; unset falls back to a `kubectl logs` snippet, no broken iframe. A follow-up (#480) pins the panel's numeric id (100) so the embed URL survives dashboard-panel reordering. Prod-rendering prerequisites (`allow_embedding`, an SSO/anon-read auth model for the iframe, the env var set, no framing block) are **not yet configured** — the feature currently degrades to the fallback everywhere. | Accepted (code live; embed prerequisites open) |
| [0111](0111-per-repo-review-settings-and-review-on-push.md) | **Per-repo review settings store + review-on-new-commits** (Epic #566) — check-run reporting, the automatic on-PR-open review, and a new review-on-new-commits trigger become individually configurable per repo via a three-layer resolution (built-in default → repo config file → DB override, operator wins), mirroring ADR-0110's precedence-chain shape. `review_on_push` defaults **off** (never silently multiplies spend); a push under `review_on_push` re-scopes ADR-0065's dedup PR-wide (bounded by an occurrence-count multiset, not a `HashSet`, so a genuinely new Nth occurrence still surfaces) and offers three push-storm strategies (`supersede`/`debounce`/`every`), `supersede` cancelling superseded runs and resolving their check to `Cancelled` rather than leaving it hanging. | Accepted |
| [0112](0112-invest-in-apps-web-supersede-0063.md) | **Invest in `apps/web` as a permanent admin surface** — supersedes ADR-0063's conclusion that `apps/web` should be retired once `clients/lci`/Grafana absorbed its functions. Since 0063, `apps/web` gained the full ADR-0024 v2 IA and the ADR-0027 daisyUI migration, and ADR-0109 already assumes it keeps working — a later ADR silently contradicting an earlier one. Chosen: both surfaces coexist permanently against the same admin API; `apps/web` is the rich structured-admin/browsing surface (starting with ADR-0111's settings UI), `clients/lci` stays the fast terminal-native ops tool — not a reversal of `lci`'s value, only of "therefore delete the web app." | Accepted |
| [0113](0113-dedicated-otel-collector-for-lci-telemetry.md) | **A dedicated OpenTelemetry Collector for LCI telemetry** — the agent-runner is a one-shot Job: it cannot be scraped and cannot report cumulative counters (all runs share one series identity, since a per-pod `service.instance.id` would multiply every series by the number of runs), so it must push delta-temporality metrics. Prod Alloy **drops delta outright**, and `otelcol.processor.deltatocumulative` is *experimental* — enabling it means `--stability.level=experimental` on the cluster's **sole** collector (all logs+traces+metrics). Rejected on blast radius; a private collector isolates the gate, the processors and the failure domain. Because Loki is on chart 7.0.0 (3.x, native OTLP ingest) it also removes the CRI-unwrap tax every dashboard query pays today. Shape: a ≥2-replica `Deployment` (a Job flushing on exit gets no retry), OTLP in, direct to Loki/Mimir/Tempo. ⚠️ Migration hazard: switching log transport changes stream labels, so the Prompt Budget dashboard renders **empty, not broken** — collector, runner and queries must land as one arc. Does NOT supersede [ai-helm-values#207](https://github.com/ADORSYS-GIS/ai-helm-values/issues/207), whose broken `level`/`trace_id` extraction is cluster-wide. | Proposed |
